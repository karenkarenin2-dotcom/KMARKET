# ВАЖНО: сборщикам (python -m kmarket.collect, python -m kmarket.collect_auction)
# зависимости НЕ нужны вообще — они написаны на стандартной библиотеке, чтобы
# GitHub Actions не тратил минуты на pip и не падал из-за чужих релизов.
# Это правило не нарушать даже ради удобства.
#
# Всё, что ниже, нужно только аналитике и приложению на локальной машине.

# Окно на WebView2. FastAPI и uvicorn отсюда убраны вместе с веб-дашбордом:
# приложению не нужен ни порт, ни сервер, ни вкладка в браузере.
pywebview==6.2.1
pandas==2.2.3
