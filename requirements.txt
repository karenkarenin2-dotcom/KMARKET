# ВАЖНО: сборщику (python -m kmarket.collect) зависимости НЕ нужны вообще —
# он написан на стандартной библиотеке, чтобы GitHub Actions не тратил
# минуты на pip и не падал из-за чужих релизов.
#
# Всё, что ниже, нужно только аналитике и дашборду на локальной машине.

fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
pandas==2.2.3
